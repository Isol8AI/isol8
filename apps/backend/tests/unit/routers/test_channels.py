"""Legacy channel router tests — retired in Phase E.

The old POST /telegram, POST /discord, POST /whatsapp/pair, GET /whatsapp/qr,
and generic DELETE /{provider} endpoints were removed in Task E1. Tests for
the new link/admin-delete endpoints live in test_channels_link_router.py.
"""
